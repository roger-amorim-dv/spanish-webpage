import type { Metadata } from "next";
export const metadata: Metadata = { title: "Acceso de estudiantes", robots: { index: false } };
export default function LoginLayout({ children }: { children: React.ReactNode }) { return children; }
